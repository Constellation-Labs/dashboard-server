apiVersion: v1
kind: Service
metadata:
  name: dashboard-server
  labels:
    app: cache
    tier: frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: http-server
  selector:
    app: cache
    tier: frontend
